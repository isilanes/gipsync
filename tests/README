# Run single test file:
#coverage run --source=. -m tests/test_core -v

# Run all tests in test/ dir:
coverage run --source=. -m unittest discover -v

# Report coverage:
coverage report -m --skip-covered
